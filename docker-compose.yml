services:
   postgres:
      image: postgres:16
      container_name: brewdesk-db
      environment:
         POSTGRES_DB: brewdesk
         POSTGRES_USER: postgres
         POSTGRES_PASSWORD: postgres
      ports:
         - "5432:5432"
